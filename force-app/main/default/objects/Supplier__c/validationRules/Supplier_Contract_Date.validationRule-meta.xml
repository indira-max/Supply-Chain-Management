<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Supplier_Contract_Date</fullName>
    <active>true</active>
    <description>End Date must be ≥ Start Date.</description>
    <errorConditionFormula>AND
   ( NOT( ISBLANK( Contract_Start_Date__c ) ) ,
     NOT( ISBLANK( Contract_Start_Date__c ) ),
   Contract_End_Date__c &lt;  Contract_Start_Date__c 
)</errorConditionFormula>
    <errorMessage>Contract End Date must be after the Start Date.</errorMessage>
</ValidationRule>
